{
  "name": "travelgpt-root",
  "version": "1.0.0",
  "description": "TravelGPT - AI Travel Planner powered by Anakin Wire and Groq",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend && npm install",
    "dev": "npx concurrently --kill-others \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start-backend": "npm start --prefix backend",
    "start-frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
